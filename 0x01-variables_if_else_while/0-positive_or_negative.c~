#include <stdio.h>

/**
 * main - printf line to stdout
 * Description: uses printf to output fixed message
 * Return: void
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
