#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - for  putchar line to stdout
* Description: uses putchar to output fixed message
* Return: 0
**/

int main(void)
{
	int n, m, w;

	for (n = 0; i < 100; n++)
	{
		m = n / 10;
		w = n % 10;

		putchar(m + '0');
		putchar(w + '0');

		if (n < 99)
		{
			putchar(,);
			putchar( );
		}
	}
	putchar('\n');

	return (0);
}
